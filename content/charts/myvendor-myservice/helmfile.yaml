releases:
  - name: '{{ requiredEnv "TENANT_ID" }}-myvendor-myservice'
    namespace: '{{ requiredEnv "TENANT_ID" }}'
    chart: ./

    values:
      - global:
          tenant:
            id: '{{ requiredEnv "TENANT_ID" }}'
            domain: '{{ requiredEnv "PUBLIC_DOMAIN" }}'

        app:
          env:
            LOGGING__FORMAT: 'Gelf'
            LOGGING__LOGLEVEL__DEFAULT: '{{ env "LOG_LEVEL" | default "Information" }}'

